// Test error handling for .len on unsupported types
print("Testing .len error handling:")

// This should work
let arr = [1, 2, 3]
print("Array length: " + toString(arr.len))

// This should cause an error
let num = 42
print("Trying to get length of number:")
print(toString(num.len))
