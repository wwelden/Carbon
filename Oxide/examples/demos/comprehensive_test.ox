// Carbon Language Comprehensive Test
// Testing syntax from Go, JavaScript, Rust, C#, Java, and C

// Basic arithmetic and variables (C/JavaScript style)
let x = 42
let y = 10
x + y

// Boolean operations (C/Java style)
let isTrue = true
let isFalse = false

// Conditional expressions (C/JavaScript style)
if (x > y) { 100 } else { 200 }

// Arrays (JavaScript style)
let numbers = [1, 2, 3, 4, 5]

// String operations (JavaScript style)
let message = "Hello Carbon"

// Mathematical constants (JavaScript Math style)
PI
E

// Type checking (JavaScript style)
typeof(x)
typeof(message)

// Ternary operator (C/JavaScript style)
x > y ? "greater" : "lesser"

// Null values (JavaScript style)
null

// Mathematical operations
x * y
x / y
x % y
x ** 2

// Comparison operations
x == 42
x != y
x >= y
x <= y

// String conversion (JavaScript style)
toString(x)

// Array length (JavaScript style)
numbers.len
